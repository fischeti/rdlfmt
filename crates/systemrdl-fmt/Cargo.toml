[package]
name = "systemrdl-fmt"
version.workspace = true
edition.workspace = true

[dependencies]
rowan = { workspace = true }
systemrdl-syntax = { path = "../systemrdl-syntax" }

# Binary only. The library half of this crate does not depend on it.
clap = { workspace = true }
